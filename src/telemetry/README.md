`simnet_telemetry` records logs, trace scopes, and raw metrics.

Import it with:

```cpp
import simnet.telemetry;
```

Use `simnet/telemetry_trace.hpp` for trace macros. Telemetry does not own benchmark scenarios, statistics, or higher-level interpretation.
